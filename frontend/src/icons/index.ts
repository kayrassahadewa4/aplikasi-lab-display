/**
 * Centralized Lucide icon exports.
 *
 * Import icons from this file instead of directly from 'lucide-vue-next'
 * to maintain a single source of truth for icon usage across the app.
 *
 * Usage:
 *   import { IconLoader, IconAlertTriangle } from '@/icons'
 */
export {
  // Navigation & Layout
  Menu as IconMenu,
  X as IconX,
  ChevronDown as IconChevronDown,
  ChevronRight as IconChevronRight,
  ChevronLeft as IconChevronLeft,
  ArrowLeft as IconArrowLeft,

  // Actions
  Plus as IconPlus,
  Pencil as IconPencil,
  Trash2 as IconTrash,
  Search as IconSearch,
  Filter as IconFilter,
  RefreshCw as IconRefresh,

  // Status & Feedback
  Loader2 as IconLoader,
  AlertTriangle as IconAlertTriangle,
  CheckCircle as IconCheckCircle,
  XCircle as IconXCircle,
  Info as IconInfo,

  // Auth & User
  LogIn as IconLogIn,
  LogOut as IconLogOut,
  User as IconUser,
  Settings as IconSettings,

  // Domain-specific
  Calendar as IconCalendar,
  Clock as IconClock,
  Building2 as IconBuilding,
  DoorOpen as IconDoor,
  LayoutDashboard as IconDashboard,
  Monitor as IconMonitor,
  ClipboardList as IconClipboardList,

  // Misc
  Eye as IconEye,
  EyeOff as IconEyeOff,
  Sun as IconSun,
  Moon as IconMoon,
} from 'lucide-vue-next'
